# =============================================================================
# mmmetric - Environment Configuration
# =============================================================================
# Copy this file to .env and configure for your deployment

# =============================================================================
# Required: Supabase Configuration
# =============================================================================
# Your Supabase project credentials (from Supabase dashboard or self-hosted instance)
VITE_SUPABASE_PROJECT_ID="your-project-id"
VITE_SUPABASE_PUBLISHABLE_KEY="your-anon-key"
VITE_SUPABASE_URL="https://your-project-id.supabase.co"

# =============================================================================
# Optional: Application Configuration (for self-hosted instances)
# =============================================================================
# Your application's public URL (defaults to window.location.origin)
# VITE_APP_URL="https://analytics.yourdomain.com"

# Custom application name for branding (defaults to "mmmetric")
# VITE_APP_NAME="Your Analytics"

# Cloud instance URL for migration prompts (only needed if forking the project)
# VITE_CLOUD_URL="https://mmmetric.lovable.app"

# =============================================================================
# Optional: Billing (Cloud Mode Only)
# =============================================================================
# Leave empty/unset for self-hosted mode (all features unlocked)
# Set to enable tiered pricing on cloud instances
# VITE_STRIPE_PUBLISHABLE_KEY="pk_live_..."

# =============================================================================
# Edge Function Secrets
# =============================================================================
# These are NOT frontend environment variables. Configure them in your Supabase
# dashboard (Edge Functions → Secrets) or via CLI: supabase secrets set KEY=value
#
# --- Required for Email Reports ---
# RESEND_API_KEY        - Your Resend.com API key for sending emails
# APP_URL               - Your application URL (for links in emails)
# APP_NAME              - Application name for email branding (default: mmmetric)
# EMAIL_FROM            - Sender address (e.g., "Analytics <reports@yourdomain.com>")
#
# --- Required for Scheduled Jobs ---
# CRON_SECRET           - Secret token for authenticating cron/scheduled requests
#
# --- Optional for Development ---
# ALLOWED_DEV_ORIGINS   - Comma-separated domains to allow tracking from
#                         (e.g., "localhost,staging.example.com")
# DAILY_SALT_SECRET     - Secret salt for visitor ID hashing (rotates daily)
#
# --- OAuth Integrations (Coming Soon) ---
# GOOGLE_CLIENT_ID      - Google OAuth client ID
# GOOGLE_CLIENT_SECRET  - Google OAuth client secret
# SHOPIFY_CLIENT_ID     - Shopify app client ID
# SHOPIFY_CLIENT_SECRET - Shopify app client secret
# ENCRYPTION_KEY        - Key for encrypting OAuth tokens at rest (use: openssl rand -hex 32)
