# Supabase Configuration
# Get these from your Supabase project settings
VITE_SUPABASE_PROJECT_ID="your-project-id"
VITE_SUPABASE_PUBLISHABLE_KEY="your-anon-key"
VITE_SUPABASE_URL="https://your-project-id.supabase.co"

# =============================================================================
# OAuth Integration Credentials (Server-side only - store in Supabase Edge Function secrets)
# =============================================================================
# These are application-level OAuth client credentials used to initiate OAuth flows.
# User-specific tokens are stored encrypted in the database.
#
# IMPORTANT: Never expose these in client-side code. Use Edge Functions to handle OAuth.
# Add these secrets via Lovable Cloud backend configuration.

# Google OAuth (for Google Analytics & Search Console integrations)
# GOOGLE_CLIENT_ID="your-google-oauth-client-id"
# GOOGLE_CLIENT_SECRET="your-google-oauth-client-secret"

# Shopify OAuth (for Shopify integration)
# SHOPIFY_CLIENT_ID="your-shopify-app-client-id"
# SHOPIFY_CLIENT_SECRET="your-shopify-app-client-secret"

# =============================================================================
# Edge Function Secrets (configure in Lovable Cloud backend)
# =============================================================================
# These should be added via the Lovable Cloud backend secrets management:
# - GOOGLE_CLIENT_ID
# - GOOGLE_CLIENT_SECRET
# - SHOPIFY_CLIENT_ID
# - SHOPIFY_CLIENT_SECRET
# - ENCRYPTION_KEY (for encrypting OAuth tokens at rest)
